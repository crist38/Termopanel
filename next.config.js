/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aumentar el límite del body de Server Actions para respuestas grandes de Odoo
  serverExternalPackages: [],
};

module.exports = nextConfig;
